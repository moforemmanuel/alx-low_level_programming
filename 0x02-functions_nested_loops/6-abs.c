#include "holberton.h"
/**
 * _abs - computes absolute value of an integer
 * int: integer to be tested
 * Return: abs
 */
int _abs(int)
{
int abs;
if (int > 0)
{
	abs = int;
}
else if (int == 0)
{
	abs = 0;
}
else
{
	abs = int*-1;
}
return (abs);
}
